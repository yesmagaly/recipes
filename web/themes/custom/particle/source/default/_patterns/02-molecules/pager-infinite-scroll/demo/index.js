/**
 * Demo of pagerInfiniteScroll. Pulls in pagerInfiniteScroll assets, and provides demo-only assets.
 *
 * (This file is NOT imported by the design system, but is included as part of
 * a Pattern Lab app.)
 */

// Import component assets
import 'molecules/pager-infinite-scroll';

// Import demo assets
import twig from './pager-infinite-scrolls.twig';
import yaml from './pager-infinite-scrolls.yml';
import markdown from './pager-infinite-scrolls.md';

export default {
  twig,
  yaml,
  markdown,
};
