/**
 * Demo of verticalFeatured. Pulls in verticalFeatured assets, and provides demo-only assets.
 *
 * (This file is NOT imported by the design system, but is included as part of
 * a Pattern Lab app.)
 */

// Import component assets
import 'molecules/vertical-featured';

// Import demo assets
import twig from './vertical-featureds.twig';
import yaml from './vertical-featureds.yml';
import markdown from './vertical-featureds.md';

export default {
  twig,
  yaml,
  markdown,
};
